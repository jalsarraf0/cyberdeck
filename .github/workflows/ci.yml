name: CI

on:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: read

jobs:
  test-cross-platform:
    name: Test (${{ matrix.os }})
    runs-on: [self-hosted, Linux, X64, docker]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run tests
        run: cargo test --all-targets

  linux-regression:
    name: Linux Full Regression
    runs-on: [self-hosted, Linux, X64, docker]
    timeout-minutes: 30
    env:
      DOCKER_RUNNER_CACHE: /tmp/docker-runner
      DOCKER_RUNNER_PRUNE: "true"
    steps:
      - uses: actions/checkout@v4
      - name: Initialize Docker runner cache
        run: mkdir -p /tmp/docker-runner/{images,cache,artifacts}
      - name: Full regression (Docker)
        run: bash .github/scripts/docker-runner.sh rust:1.93-bookworm "cargo fmt --all -- --check && cargo test --all-targets && ./scripts/regression.sh"
